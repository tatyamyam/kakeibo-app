# kakeibo-app

家計簿アプリのプロジェクトです。

## プロジェクト概要

収支の記録・管理を行う家計簿アプリケーションです。

## Git 運用ルール

**コードを変更するたびに必ず GitHub へプッシュしてください。**

### 基本フロー

1. 変更を加える
2. 変更内容を確認する: `git status` / `git diff`
3. 関連ファイルをステージング: `git add <ファイル名>`
4. コミットメッセージは変更の「なぜ」を簡潔に記述する
5. **即座にプッシュする**: `git push origin main`

### コミットメッセージの形式

```
<種別>: <変更の概要>

例:
feat: 支出カテゴリの追加機能を実装
fix: 月次集計の計算ミスを修正
refactor: 収支一覧のコンポーネントを整理
docs: READMEにセットアップ手順を追記
```

### 禁止事項

- `--no-verify` でフックをスキップしない
- `git push --force` を main ブランチに使用しない
- 動作確認前にプッシュしない

## コーディング規則

- コメントは「なぜそうしているか」が非自明な場合のみ記述する
- 不要な抽象化・将来の拡張のための設計はしない
- エラーハンドリングはユーザー入力・外部 API のシステム境界のみに追加する
